# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_public_key

# Canonical site URL for metadata / Open Graph (no trailing slash). Use your BuckParts domain in production.
NEXT_PUBLIC_SITE_URL=https://your-buckparts-domain.example

# Server-side read-only live-site smoke target for Daily Operator / live-site checks.
# Prefer the real BuckParts production custom domain; do not use this as deploy proof.
LIVE_SITE_SMOKE_TARGET_URL=https://buckparts.com
# Optional alias for the business public domain; LIVE_SITE_SMOKE_TARGET_URL wins when both are set.
BUCKPARTS_PUBLIC_SITE_URL=https://buckparts.com

# Optional: override the public product name in the header and default metadata (default: BuckParts)
# NEXT_PUBLIC_SITE_NAME=BuckParts

# Server-only: private owner dashboard (`/ownerdashboard/<secret>`). Use a long random value; never commit a real secret.
OWNER_DASHBOARD_SECRET=

# Server-only: CSV import scripts (scripts/import-seed.ts). Never expose in the browser.
SUPABASE_SERVICE_ROLE_KEY=your_service_role_secret_key

# Server-only: durable owner-report artifact storage (Supabase table owner_report_artifacts).
# Required in production so lane 16 can read shared durable artifacts across Netlify instances.
# Reuses NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY.

# Server-only: Google Search Console scheduled artifact fetch (scripts/fetch-buckparts-gsc-artifact.ts).
# Auth mode priority:
# 1) OAuth refresh token mode (recommended when service-account key creation is blocked):
#    - GSC_OAUTH_CLIENT_ID
#    - GSC_OAUTH_CLIENT_SECRET
#    - GSC_OAUTH_REFRESH_TOKEN
# 2) Service account mode:
#    - GSC_SERVICE_ACCOUNT_JSON as single-line JSON string, OR
#    - GSC_SERVICE_ACCOUNT_KEY_PATH as absolute path on the server (do not commit key files).
# All values are server-only secrets; never expose in browser/client code.
GSC_PROPERTY_SITE_URL=
GSC_OAUTH_CLIENT_ID=
GSC_OAUTH_CLIENT_SECRET=
GSC_OAUTH_REFRESH_TOKEN=
GSC_SERVICE_ACCOUNT_JSON=
GSC_SERVICE_ACCOUNT_KEY_PATH=

# Client-side optional GA4 page/event collection bootstrap (src/components/AnalyticsScripts.tsx).
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Server-only: GA4 trust-funnel scheduled artifact fetch (scripts/fetch-buckparts-ga4-trust-funnel-artifact.ts).
# Stage 1 supports event-name totals and rates only.
# Preferred auth mode: OAuth refresh token.
GA4_PROPERTY_ID=
GA4_OAUTH_CLIENT_ID=
GA4_OAUTH_CLIENT_SECRET=
GA4_OAUTH_REFRESH_TOKEN=
# Optional parity auth mode when key management allows it:
GA4_SERVICE_ACCOUNT_JSON=
GA4_SERVICE_ACCOUNT_KEY_PATH=
